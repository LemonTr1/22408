---
tags: [考研/408/数据结构, 错题本]
---

# 数据结构 · 错题本

> 用法：每错一题填一行。**「正解要点」必须写成一句话能复述的结论**（不是「见笔记」）。
> 复习时只看「正解要点」列，能立刻说出结论才算过。

## 错题索引

| 序 | 知识点 | 题目出处 | 正解要点 | 已订正 |
|---|---|---|---|---|
| 1 | 绪论与算法分析 | 全国联考 2022 · 第 2 题（双层循环 `i *= 2` / `j < i`） | **O(n)**：外层 ⌈log₂n⌉ 次，内层上限为变量 i，等比求和 1+2+…+2^k ≈ 2n，看末项得线性 | ⬜ |
| 2 | 线性表 | | | ⬜ |
| 3 | 栈队列数组 | 题号 47（广义表表尾与深度） | `tail(L)=((e),(a,(b,c,d)))`、`depth(L)=`**3**：**表尾必是表**（剩余元素再套括号）；**空表深度为 1**、原子为 0，`depth=1+max(元素深度)`<br>🔎 **同章**：中缀转后缀（`x+y*(z-u)/v`）→ 栈法：**当前运算符优先级高于栈顶才入栈，否则先弹栈**；`/` 遇同级栈顶 `*` 要先弹出，得 `x y z u - * v / +`（选 **A**）。方法见 [[数据结构_03_栈、队列和数组#🔢 计算方法：中缀表达式 → 后缀表达式（逆波兰式）]] | ⬜ |
| 4 | 串/KMP | 模式串 `aabaab`，用修正后的 **nextval** 匹配，某位失配时最长滑动距离 | next = `[-1,0,1,0,1,2]`；修正：`S[j]==S[next[j]]` 时取 `nextval[next[j]]`，否则取 `next[j]` → nextval = `[-1,-1,1,-1,-1,1]`。滑动距离 = **`j − nextval[j]`**，逐位 `1,2,1,4,5,4`，最大 **5**（选 **A**）。方法见 [[数据结构_04_串#🔢 计算方法：next 与 nextval（修正 next）数组]] | ⬜ |
| 5 | 树与二叉树 | 题号 9 · 全国联考 2020（森林 F 转二叉树 T，由先根/后根推后序） | F 先根 = T **先序**、F 后根 = T **中序**，再用「先序+中序 → 后序」还原：`b,f,e,d,c,a`（选 **C**）。**后序末位必是根**<br>🔎 **同章**：全国联考 2013（三叉哈夫曼，叶权 2,3,4,5,6,7）→ 先补 1 个权 0 虚叶，三次合并 5+14+27，WPL=**46**（选 **B**）。**(n−1) mod (m−1) ≠ 0 就要补虚叶**；根权 27 不是 WPL | ⬜ |
| 6 | 图 | | | ⬜ |
| 7 | 查找 | 全国联考 2023（散列表线性探查插入 3 个关键字后删除，求 ASL_失败） | 删除必须打 **DEL 标记**，不能置空；求 ASL_失败 时**遇 DEL 不停**、探到 NULL 才停（NULL 那次也计数），绕回照样探。各入口次数 1,3,2,1,2 → **9/5 = 1.8**（选 **C**）；若误按置空算得 1.6 即落陷阱 B<br>🔎 **同考点**：全国联考 2019（m=11、H=key%7、插 8 个关键字）→ ASL_失败 = (9+8+7+6+5+4+3)/**7** = **6**（选 **C**）。**分母是模数 p = 7，不是表长 m = 11** | ⬜ |
| 8 | 排序 | 全国联考 2015（小根堆 8,15,10,21,34,16,12 删除堆顶 8 后重建堆的比较次数） | 末尾结点补堆顶后**自上而下**下滤：先比左右孩子（1 次）→ 与较小者比（1 次）→ 与单孩子 16 比（1 次），共 **3** 次（选 **C**）。**插入上滤自下而上、删除下滤自上而下，方向别记反**<br>🔎 **同章**：全国联考 2019（120 个初始归并段、12 路归并求补虚段数）→ 最佳归并树即 **m 叉哈夫曼树**，`(120−1) mod (12−1)=9≠0` → 补 `11−9=2` 个虚段（选 **B**）；公式见 [[数据结构_05_树与二叉树#🔢 计算方法：m叉哈夫曼树]]<br>🔎 **同章**：全国联考 2022（选直接插入而非快排的原因）→ Ⅰ 越有序越快、Ⅱ 小规模高效、Ⅲ 插入 O(1) 空间、Ⅳ 插入稳定，四条全成立（选 **D**）。**快排空间 O(log n) 来自递归栈**<br>🔎 **同章**：外部排序·败者树 → 结点只存**归并段号**；记录「冠军」的结点保存的是**最小关键字所在的归并段号**（选 **D**）；方法见 [[数据结构_08_排序#🔢 计算方法：败者树（k 路归并选极值）]]<br>🔎 **同章**：全国联考 2023（置换-选择排序生成初始归并段，m=4、19 个记录）→ 循环「**选 WA 中最小者输出 → 读入下一条，≥ 刚输出者留本段，< 则「冻结」留待下一段**」（冻结记录**仍占工作区**；**全部冻结**才结束本段）。得 **3** 段：`37 51 63 92 94 99`／`14 15 23 31 48 56 60 90 166`／`8 17 43 100`。首段长度**最大 = n**（输入已有序则永不冻结）、**最小 = m**（前 m 个恰为全局最大 m 个则后续全被冻结）；方法见 [[数据结构_08_排序#🔢 计算方法：置换-选择排序（生成初始归并段）]] | ⬜ |
| 9 | 算法设计题（线性表·单链表） | 全国联考 2012（带头结点单链表，两单词共享后缀，找共同后缀起始位置） | **双指针「表尾对齐」**：各遍历一次求长度 m、n → **长链先走 \|m−n\| 步** → 两指针同步后移，**首次 `p == q`（比较结点地址，不是 data）** 即公共结点，无则返 NULL。**O(m+n) 时间、O(1) 空间**（不借助数组/栈 = 满分关键）。方法见 [[数据结构_02_线性表#🔢 解题模板：双指针找公共结点（表尾对齐）]] | ⬜ |

## 错因分布自查

| 错因类型 | 计数 | 对策 |
|---|---|---|
| 概念不清 | | 回看知识点笔记 |
| 计算失误 | | 限时训练、验算习惯 |
| 审题遗漏 | | 读题划关键词 |
| 方法不会 | | 补题型模板 |

